import User from './User';
import Settings from './Settings';
import Bot from './Bot';

// Define associations here if needed

export { User, Settings, Bot };

// Export a default object with all models
export default {
  User,
  Settings,
  Bot,
};

