import User from './User';
import Settings from './Settings';
import Bot from './Bot';
import OrderHistory from './OrderHistory';

// Define associations here if needed

export { User, Settings, Bot, OrderHistory };

// Export a default object with all models
export default {
  User,
  Settings,
  Bot,
  OrderHistory,
};

